import { createMuiTheme } from '@material-ui/core/styles';

export const theme = createMuiTheme({
    palette: {
        primary: {
            light: '#007bdc',
            main: '#007bdc',
            dark: '#007bdc',
            contrastText: '#fff',
        },
        secondary: {
            light: '#f1c40f',
            main: '#f1c40f',
            dark: '#f1c40f',
            contrastText: '#fff',
        },
    },
});